// Kimi uses the shared one-shot ACP permission bridge. Keep the old export
// name so the launcher and its mocks remain flavor-local.
export { AcpPermissionHandler as KimiPermissionHandler } from '@/modules/common/permission/AcpPermissionHandler'
